import 'package:flutter/material.dart';

class Receive_opt extends StatefulWidget {
  @override
  Receive_opt_state createState() => Receive_opt_state();
}

class Receive_opt_state extends State<Receive_opt> {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        backgroundColor: Color.fromARGB(255, 163, 230, 165),
        title: Text("receive_opt"),
      ),
      body: Center(
        child: Text("receive_opt"),
      ),
    );
  }
}
